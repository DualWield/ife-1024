define(function (require) {
    let Draw = require('Draw');
    return class Plane {
        constructor () {
            this.x = 0;
            this.y = 0;
        }

        updatePosition (x, y) {
            this.x = x;
            this.y = y;
        }

        update () {
            Draw.
        }
    }
});